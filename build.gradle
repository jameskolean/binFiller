apply plugin: 'java'

repositories {
    jcenter()
}

dependencies {
    compile 'org.slf4j:slf4j-api:1.7.21'

    testCompile 'junit:junit:4.12'
}
task hello(type: GreetingTask){
	greeting = 'howdy!'
}
task changedTest(){
    inputs.file file('trigger.txt')
    outputs.dir file("$buildDir/generated")
	doLast {
      file("$buildDir/generated").mkdirs()
	  println 'Yeah Im running'
	}
}
